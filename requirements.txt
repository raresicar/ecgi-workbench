# ECGi Workbench — app-layer dependencies (pip-installable).
#
# NOTE: the heavy scientific stack (dolfinx, petsc4py, mpi4py, gmsh, ufl) is NOT
# listed here because it cannot be pip-installed reliably — it must come from the
# conda environment used for the thesis (see README.md). These are only the extra
# packages the interactive UI needs on top of that environment.
streamlit>=1.40
plotly>=5.20
numpy
scipy
